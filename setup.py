from setuptools import setup, find_packages

setup(
    name='python-ed-fcad-uner',
    packages=find_packages(where='data_structures'),
    package_dir={'': 'data_structures'}
    description='',
    version='1.0.0',
    author='nacho <nacho.novello@gmail.com>'
    install_requires=[],
    extras_require={},
    keywords=['python', 'data structures', 'graph', 'trees', 'stack', 'queue']
)